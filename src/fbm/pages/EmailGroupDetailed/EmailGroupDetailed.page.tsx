import React, { memo } from 'react';
import { EmailGroupDetailed } from '../../components/EmailGroupDetailed/EmailGroupDetailed.component';

export const EmailGroupDetailedPage = memo(() => {
  return <EmailGroupDetailed />;
});
