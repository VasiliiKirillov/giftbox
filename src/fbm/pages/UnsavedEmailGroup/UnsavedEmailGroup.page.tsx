import React, { memo } from 'react';

import { UnsavedEmailGroup } from '../../components/UnsavedEmailGroup/UnsavedEmailGroup.component';

export const UnsavedEmailGroupPage = memo(() => {
  return <UnsavedEmailGroup />;
});
